import React from "react";
import { ProfileTitle } from "./Elements for Profile/ProfileTitle";
import { ProfileInfo } from "./Elements for Profile/ProfileInfo";

export const ProfilePage = () => {
  return (
    <div>
      <ProfileTitle />
      <ProfileInfo />
    </div>
  );
};
